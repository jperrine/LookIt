Settings for <%= @user.display_name %>
<% form_for(@user, :html => { :multipart => true }) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'user_form', :locals => { :f => f }%>
  <%= submit_tag('Update')%>
<% end %>