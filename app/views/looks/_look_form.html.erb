<% form_for([@current_user, @look])  do |f| %>
	<%= f.error_messages %>
	<%= f.label :title, "Look Title" %> <%= f.text_field :title %>
	<br />
	<%= f.label :content, "Look Description" %>
	<br />
	<%= f.text_area :content %>
	<br />
	<%= f.label :cover %><%= f.file_field :cover %><br />
	<br />
	<%= submit_tag("Save") %>
<% end %>