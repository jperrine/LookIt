<%= render :partial => 'looks/look_summary', :locals => {:look => @page.look} %>
<div id= "canvas"> 
	<%= render :partial => 'looks/look_page_select', :locals => { :look => @page.look } %>
  <div id= "TextArea">
		<div id="content">
			<div id="page_editor_controls">
				<%= link_to "Edit Page", edit_user_look_page_url(@page.look.user, @page.look, @page) %> | 
				<%= link_to "Add Page", new_user_look_page_url(@page.look.user, @page.look) %> | 
				<%= link_to "Delete Page", user_look_page_url(@page.look.user, @page.look), :method => 'delete', :confirm => 'Are you sure you want to delete this page?' %>
			</div>
			<%= @page.content %>
		</div>
  </div> 
	<a id="LastPage" href="#">< Last Page</a> 
	<a id="NextPage" href="#">Next Page ></a> 
</div>